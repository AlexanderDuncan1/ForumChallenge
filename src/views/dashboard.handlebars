{{#each blogs}}
  <div class="blog-post">
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
    <p>Posted by {{this.author.username}} on {{this.createdAt}}</p>
    <a href="/blogs/{{this.id}}/edit">Edit</a>
  </div>
{{/each}}
<a href="/dashboard/new">Create New Post</a>